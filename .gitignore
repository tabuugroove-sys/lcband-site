.DS_Store
*.log
__MACOSX/
._*
Thumbs.db
.claude/
node_modules/
_site/

# Production video is served by Sprinthost. Keep local working copies on the
# Mac, but never commit MP4 binaries to Git again.
src/assets/video/mp4/*.mp4

# Nano Banana backups (keep local only)
src/assets/.nanobanana-backup/

# Dev tools (not for public)
tools/*
!tools/seo-audit.mjs
!tools/agent-gate.sh
!tools/clean-site.mjs
!tools/media-manifest.mjs
!tools/upload-video.mjs
!tools/hooks/
!tools/hooks/pre-push
output/

# Multi-agent coordination lock (local only, never commit)
.agent-lock
.local-backups/
.indexnow-key
.clone-ui/

# Playwright test artifacts (local only)
playwright-report/
test-results/
