<!DOCTYPE html>
<html lang="{{ htmlLang or 'ru' }}"{% if htmlDir %} dir="{{ htmlDir }}"{% endif %}>
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
	<title>{{ title or site.name }}</title>
	<meta name="description" content="{{ description or site.description }}">
	{% if keywords %}<meta name="keywords" content="{{ keywords }}">{% endif %}
	<link rel="canonical" href="{{ site.url }}{{ page.url }}">
	<meta name="theme-color" content="#000000">
	<meta name="format-detection" content="telephone=no">

	<meta property="og:type" content="website">
	<meta property="og:locale" content="{{ ogLocale or 'ru_RU' }}">
	<meta property="og:title" content="{{ title or site.name }}">
	<meta property="og:description" content="{{ description or site.tagline }}">
	<meta property="og:image" content="{{ site.url }}/{{ ogImage or site.defaultOgImage }}">
	<meta property="og:site_name" content="{{ site.name }}">
	<meta property="og:url" content="{{ site.url }}{{ page.url }}">

	<meta name="twitter:card" content="summary_large_image">
	<meta name="twitter:title" content="{{ title or site.name }}">
	<meta name="twitter:description" content="{{ description or site.tagline }}">
	<meta name="twitter:image" content="{{ site.url }}/{{ ogImage or site.defaultOgImage }}">

	{# hreflang: четыре языковые версии главной (RU — основная, /ae/ AR, /br/ PT, /en/ EN).
	   Только на этих четырёх URL, чтобы RU/AR/PT/EN не конфликтовали в выдаче #}
	{% if page.url == '/' or page.url == '/ae/' or page.url == '/br/' or page.url == '/en/' %}
	<link rel="alternate" hreflang="ru" href="{{ site.url }}/">
	<link rel="alternate" hreflang="ar" href="{{ site.url }}/ae/">
	<link rel="alternate" hreflang="pt-BR" href="{{ site.url }}/br/">
	<link rel="alternate" hreflang="en" href="{{ site.url }}/en/">
	<link rel="alternate" hreflang="x-default" href="{{ site.url }}/">
	{% endif %}
	{% if page.url == '/sax/' or page.url == '/en/sax/' %}
	<link rel="alternate" hreflang="ru" href="{{ site.url }}/sax/">
	<link rel="alternate" hreflang="en" href="{{ site.url }}/en/sax/">
	<link rel="alternate" hreflang="x-default" href="{{ site.url }}/sax/">
	{% endif %}
	{# Черновики/превью дизайна: исключены из выдачи, но доступны по прямой ссылке #}
	{% if noindex %}<meta name="robots" content="noindex,nofollow">{% endif %}
	<link rel="icon" type="image/svg+xml" href="{{ '/assets/favicon.svg' | url }}">
	<link rel="mask-icon" href="{{ '/assets/favicon.svg' | url }}" color="#000000">
	{# preloadImage может быть с ведущим слэшем и без — нормализуем к абсолютному,
	   иначе на /ae/ и /br/ preload резолвится в /ae/assets/... и бьёт в 404.
	   preloadHeroMobileWebp: на мобильном hero отдаётся webp из <picture> —
	   прелоадим его, а jpg прелоадим только на десктопе (без двойной загрузки) #}
	{% if preloadHeroMobileWebp %}
	<link rel="preload" as="image" href="{{ ('/' + preloadImage) | replace('//', '/') | url }}" media="(min-width: 735px)">
	<link rel="preload" as="image" media="(max-width: 734px)" imagesizes="100vw"
		imagesrcset="{{ '/assets/hero/hero-red-640.webp' | url }} 640w, {{ '/assets/hero/hero-red-960.webp' | url }} 960w, {{ '/assets/hero/hero-red-1280.webp' | url }} 1280w">
	{% elif preloadImage %}<link rel="preload" as="image" href="{{ ('/' + preloadImage) | replace('//', '/') | url }}">{% endif %}
	<link rel="stylesheet" href="{{ '/styles.css?v=38' | url }}">

	<script>window.SITE_BASE = "{{ '/' | url }}";</script>

	{% block schema %}
	<script type="application/ld+json">
	{
		"@context": "https://schema.org",
		"@graph": [
			{
				"@type": "MusicGroup",
				"@id": "{{ site.url }}/#musicgroup",
				"name": "{{ site.name }}",
				"alternateName": [{% if htmlLang == 'en' %}"{{ site.shortName }}", "Luxury Cover Band"{% else %}"{{ site.shortName }}", "Лакшери Кавер Бэнд", "кавер-группа Luxury Cover Band"{% endif %}],
				"description": "{{ description or site.description }}",
				"genre": ["Pop", "Rock", "Jazz", "Soul", "Funk", "Retro", "Latin", "Italian", "French chanson"],
				"image": "{{ site.url }}/{{ site.defaultOgImage }}",
				"url": "{{ site.url }}",
				"sameAs": [
					"{{ site.telegram }}"
					{% if site.youtube %}, "{{ site.youtube }}"{% endif %}
					{% if site.vk %}, "{{ site.vk }}"{% endif %}
					{% if site.instagram %}, "{{ site.instagram }}"{% endif %}
					, "https://uslugi.yandex.ru/profile/LuxuryCoverBand-2278177"
					, "https://profi.ru/profile/treda_2177/"
					, "https://moskva.artist.ru/Luxury_Cover_Band/"
				],
				"areaServed": [{% if htmlLang == 'en' %}"Moscow", "Russia", "International tours"{% else %}"Москва", "Россия", "Международные туры"{% endif %}]
			},
			{
				"@type": "LocalBusiness",
				"@id": "{{ site.url }}/#business",
				"name": "{{ site.name }}",
				"description": "{{ description or site.description }}",
				"image": "{{ site.url }}/{{ site.defaultOgImage }}",
				"url": "{{ site.url }}",
				"address": {
					"@type": "PostalAddress",
					"addressLocality": "{% if htmlLang == 'en' %}Moscow{% else %}{{ site.city }}{% endif %}",
					"addressCountry": "RU"
				},
				"areaServed": [{% if htmlLang == 'en' %}"Moscow", "Moscow Region", "Russia", "International tours"{% else %}"Москва", "Московская область", "Россия", "Международные туры"{% endif %}],
				"priceRange": "$$",
				"sameAs": [
					"{{ site.telegram }}"
					{% if site.youtube %}, "{{ site.youtube }}"{% endif %}
					{% if site.vk %}, "{{ site.vk }}"{% endif %}
					{% if site.instagram %}, "{{ site.instagram }}"{% endif %}
				]
			},
			{
				"@type": "WebSite",
				"@id": "{{ site.url }}/#website",
				"name": "{{ site.name }}",
				"url": "{{ site.url }}",
				"inLanguage": "{% if htmlLang == 'en' %}en-US{% else %}ru-RU{% endif %}",
				"publisher": { "@id": "{{ site.url }}/#musicgroup" }
			}
		]
	}
	</script>
	{% endblock %}
	{% block head %}{% endblock %}

	<!-- Yandex.Metrika counter -->
	<script type="text/javascript">
		if (!/^(localhost|127\.0\.0\.1|\[::1\])$/.test(location.hostname)) {
			(function(m,e,t,r,i,k,a){
				m[i]=m[i]||function(){(m[i].a=m[i].a||[]).push(arguments)};
				m[i].l=1*new Date();
				for (var j = 0; j < document.scripts.length; j++) {if (document.scripts[j].src === r) { return; }}
				k=e.createElement(t),a=e.getElementsByTagName(t)[0],k.async=1,k.src=r,a.parentNode.insertBefore(k,a)
			})(window, document,'script','https://mc.yandex.ru/metrika/tag.js?id=110040914', 'ym');

			ym(110040914, 'init', {ssr:true, webvisor:true, clickmap:true, ecommerce:"dataLayer", referrer: document.referrer, url: location.href, accurateTrackBounce:true, trackLinks:true});
		}
	</script>
	<noscript><div><img src="https://mc.yandex.ru/watch/110040914" style="position:absolute; left:-9999px;" alt="" /></div></noscript>
	<!-- /Yandex.Metrika counter -->
</head>
<body>

	{% include "partials/nav.njk" %}

	<main id="top">
		{% block content %}{{ content | safe }}{% endblock %}
	</main>

	{% include "partials/footer.njk" %}
	{% include "partials/lightbox.njk" %}

	<script src="{{ '/main.js?v=16' | url }}" defer></script>
</body>
</html>
